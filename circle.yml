machine:
  java:
    version: openjdk7
checkout:
  post:
    - echo "maven.repopath=./build/maven" > ./gradle.properties
deployment:
  maven:
    branch: master
    commands:
      - chmod +x ./deploy_to_maven.sh
      - ./deploy_to_maven.sh